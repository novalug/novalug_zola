# The URL the site will be built for
base_url = "https://novalug.org/"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

# theme
theme = "terminimal"

title = "NoVaLUG"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
# Put all your custom variables here

# terminimal theme configs
accent_color = "green"
background_color = "dark"
logo_text = "Northern Virginia Linux Users Group (NoVaLUG)"
page_titles = "combined"
menu_items = [
    # each of these is optional, name and url are required
    # $BASE_URL is going to be substituted by base_url from configuration
    {name = "Home", url = "$BASE_URL"},
    {name = "About", url = "$BASE_URL/about"},
    {name = "Meetings", url = "$BASE_URL/meetings"},
    {name = "Presentations", url = "$BASE_URL/presentations"},
    {name = "Area Events", url = "$BASE_URL/events"},
    {name = "Pictures", url = "$BASE_URL/pics"},
    {name = "Link(ed) List", url = "$BASE_URL/linklist"}
    
]
favicon = "/penguin_novalug.png"
favicon_mimetype = "image/png"
